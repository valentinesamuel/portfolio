export interface Project {
      name: string;
      stack: string[];
      repo: string;
      liveDemo: string;
}
export interface ArchivedProject{
      name: string;
      repo: string;
      livePage: string;
      
}